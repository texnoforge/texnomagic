click               # clean CLI library
jsonrpcserver >= 5  # JSON-RPC server
parsimonious        # language parsing (PEG)
pyyaml              # YAML format support
requests            # HTTP for humans
rich                # terminal colors
scikit-learn        # symbol model training and recognition
toml                # TOML format support
